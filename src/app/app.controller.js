'use strict'; // NOSONAR

angular.module('POC')
    .controller('ApplicationController',
        ($scope, $rootScope, $location, $window, $state) => {
            'ngInject'; // NOSONAR
            
    });